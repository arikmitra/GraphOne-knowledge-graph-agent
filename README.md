# GraphOne-knowledge-graph-agent
multi-source ETL pipeline for an AI industry knowledge graph — scrapes startups, products, research papers, jobs, and news; extracts structured data via a multi-tier LLM fallback chain; resolves entity names (with an optional web-verification fallback for ambiguous cases); and validates every record before output.
